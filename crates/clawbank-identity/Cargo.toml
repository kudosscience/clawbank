[package]
name = "clawbank-identity"
version = "0.1.0"
edition = "2021"

[dependencies]
data-encoding = "2"
libp2p-identity = { version = "0.2", features = ["ed25519", "peerid", "rand"] }

[dev-dependencies]
tempfile = "3"
