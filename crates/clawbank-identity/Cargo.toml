[package]
name = "clawbank-identity"
version = "0.1.0"
edition = "2021"
rust-version = "1.83"
publish = false

[dependencies]
data-encoding = "2"
fs2 = "0.4"
libp2p-identity = { version = "0.2", features = ["ed25519", "peerid", "rand"] }

[dev-dependencies]
tempfile = "3"
